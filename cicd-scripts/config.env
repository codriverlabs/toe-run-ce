# Build Configuration
# Override these values by setting environment variables or editing this file

# Project settings
PROJECT_NAME="toe-k8s-operator"
VERSION="${VERSION:-v1.0.47}"
NAMESPACE="${NAMESPACE:-toe-system}"

# Registry settings (choose one)
REGISTRY_TYPE="${REGISTRY_TYPE:-local}"  # Options: local, ecr

# Local registry (microk8s)
LOCAL_REGISTRY="${LOCAL_REGISTRY:-localhost:32000}"

# ECR settings
ECR_ACCOUNT_ID="${ECR_ACCOUNT_ID:-864899852480}"
ECR_REGION="${ECR_REGION:-eu-west-1}"
ECR_REGISTRY="${ECR_REGISTRY:-${ECR_ACCOUNT_ID}.dkr.ecr.${ECR_REGION}.amazonaws.com}"

# Image pull secret name
SECRET_NAME="${SECRET_NAME:-ecr-secret}"

# Build options
SKIP_TESTS="${SKIP_TESTS:-false}"
FORCE_REBUILD="${FORCE_REBUILD:-false}"
