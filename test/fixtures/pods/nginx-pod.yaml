apiVersion: v1
kind: Pod
metadata:
  name: nginx-target
  namespace: default
  labels:
    app: nginx
    env: testing
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
